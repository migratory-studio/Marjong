node_modules/
.claude/
.DS_Store
Thumbs.db
*.log

# Cloudflare wrangler のローカル状態（DOストレージ/キャッシュ）
.wrangler/

# 背景アセットの生ライブラリ（DLツール成果物含む・本番には厳選分のみ graphic/bg/ に配置）
background/
# 立ち絵グリッド等の作業用一時ファイル
tmp/
# Playwright MCP の作業成果物（スナップショット/スクショ）
.playwright-mcp/
# デバッグ用スクラッチ出力
_*.txt

# コンテンツ生成プロジェクト（master駆動・別管理）。正典の reference のみ参照し本体では追跡しない。
scenario-forge/

# 素材置き場（原本ストック）。購入素材の生パック（PSD・利用規約含む）は再配布防止のため追跡しない。
# ポリシー: ここは「未使用の原本」。実際に使う素材だけ graphic/（追跡対象）へ移動/コピーして使う。
_assets-stock/
