# キャラ別インゲーム演出 — 第1弾：アタッカー陣（実装済み）

> **状況（2026-08-23）：本書のアタッカー編は実装完了**。§5 に「どこに何があるか」の実装メモ。
> 第2弾（ブロッカー／ギャンブラー／アビス）は未着手。

> 判断軸は CLAUDE.md（愛着4原理＝**蓄積 × 固有性 × 双方向 × 反転**／点棒=HPの体感）。
> オートバトル側のジュース候補は `docs/juice_backlog.md`。本書は**対局中（本気対局）でキャラが立つか**だけを扱う。
> 対象＝アタッカー5名（詩玥・ルクス・ゼロ・姚玖・春嬋・篠宮 栞）。ブロッカー／ギャンブラー／アビスは第2弾以降。

---

## 0. 現状の棚卸し（実装の事実）

| キャラ | 能力 | いま卓上で起きていること |
|---|---|---|
| 詩玥 | 幸運のツモ（発動局は毎ツモ有利牌へ偏る） | 発動カットイン → **毎ツモ、ツモ牌に金のリングFX**（`playLuckyTsumoFx` src/main.js:6383）→ 局中セリフ |
| ルクス・ゼロ | ゼロ・リサーチ（1シャンテン→確定聴牌） | カットイン → action-bar に候補ボタン2つ（`showLuxCandidates` src/main.js:4872）→ 次ツモで牌が来る（**来た瞬間は無演出**） |
| 姚玖 | 老頭の庭（么九に偏る） | カットイン **のみ**。以後、局が終わるまで何も起きない |
| 春嬋 | 韋駄天の中張（2〜8に偏る） | カットイン **のみ**。以後、局が終わるまで何も起きない |
| 篠宮 栞 | 模範解答（有効打トップ3を①②③） | 手牌に丸数字（`_bestMark`）＋Lv7以上で左上に板書HUD（`updateModelAnswerHud` src/main.js:5017） |

### 不足の正体は3種類ある（処方箋が違う）

- **(A) 持続が見えない** … 姚玖・春嬋。発動したのに世界が変わらない。「使ったのに何も起きなかった」体験。
- **(B) 因果が見えない** … ルクス。選択はできるが、**選んだ牌が来た瞬間**＝回収のカタルシスが無い。
- **(C) 機能はあるがキャラがいない** … 栞。誰が作ってもいい汎用UI。能力が便利なほどキャラが消える。

詩玥だけが (A) を解けている＝金リング。**「詩玥だけ実装されている」という体感は正しい**。

---

## 1. 今回の物差し

> **能力＝そのキャラが見ている世界を、一局だけプレイヤーに貸す。**

演出を「効果の説明」ではなく「**視界の貸し出し**」として作る。そうすると愛着4原理の *固有性* が自動的に立つ。

- 詩玥 ＝「ツモは、光る」
- 姚玖 ＝「端の牌は、灯り」
- 春嬋 ＝「卓は、駆ける道」
- ルクス ＝「山は、走査対象」
- 栞 ＝「卓は、教室」

**禁じ手**：効果の数値をそのまま出すこと（「么九率+40%」等）。世界の貸し出しにならず、キャラが消える。

---

## 2. キャラ別プラン

### 2-1. 姚玖「老頭の庭」— 夜の庭に、灯がともる  ✅実装済み

一人称「俺」・朴訥クール・口癖「揃うと、いいな」。**庭番（先代の養子）＝守る側**。能力は短期戦で当たりにくい仕様（だから初期16,000）。
つまり**当たらなくても進捗が見える装置**が要る。灯が増えること自体を報酬にする。

| # | 演出 | 原理 | 実装アンカー | コスト |
|---|---|---|---|---|
| 1 | **発動＝卓が夜になる**。四隅がすっと暗く落ち、自分の手牌の下辺に石畳＋灯籠の細い帯（庭）が出る。局が終わるまで残る＝持続の器 | 固有性 | DOM オーバーレイ（`tsumo-fx` と同じ position:fixed 方式）＋ CSS | M |
| 2 | **么九を引くたび灯が1つともる**。手牌の么九「種類数」＝灯籠の数。点灯の瞬間、その牌に橙のリング（詩玥の金と対の色） | 蓄積 | `maybePlayLuckyTsumoFx` と同型の hook（ツモ後に種類数を数えて差分点灯） | S |
| 3 | **夢の射程が見える**。么九が9種を超えると庭全体が明るくなる。国士テンパイで**満月**が昇り、「……揃うと、いいな」のテロップ | 反転 | `showSeatCall`（src/main.js:5493）流用＋庭レイヤーの状態切替 | S |
| 4 | **局後1行**：「今夜の灯り ◯／13」。派手にせず、静かに1行だけ | 蓄積 | 和了/流局の結果表示に1行差す | S |

> **なぜ効くか**：姚玖の能力は「当たらないこと」が仕様に織り込まれている。従来は当たらない＝無。灯なら**引けなかった局も庭が暗いまま**という語りになる。夢追いの体感がメカと一致する。

---

### 2-2. 春嬋「韋駄天の中張」— 走った距離が、見える  ✅実装済み

一人称「うち」・早口きびきび・口癖「間に合わせる」。姚玖を「兄さん」と呼ぶ。
中張が来ること自体は**当たり前すぎて気づけない**。だから「速度」という別チャネルに翻訳する。**時間で体感させる**のは他キャラに無い軸。

| # | 演出 | 原理 | 実装アンカー | コスト |
|---|---|---|---|---|
| 1 | **ツモが速くなる**。発動中は自分のツモ牌のスライドインを短縮＋牌の後ろに細い風ライン。ツモSEも半音上げ | 固有性 | 描画側のツモ演出タイミング＋`audio.playSe` の差し替え | M |
| 2 | **足跡カウンタ**。シャンテンが進むたび席の横に「一歩」「二歩」…と足跡テロップ。3歩連続で「韋駄天」バッジ点灯、以後さらにテンポが詰まる | 蓄積 | `spawnCall`（src/main.js:5482）＋ `humanShanten()`（src/main.js:8180）の差分監視 | S |
| 3 | **聴牌の瞬間**：「——間に合った」＋風が抜ける（画面端を横切る一閃）。口癖の回収 | 反転 | 既存 `tenpai` イベント（`setupMatchTalk`）にFXを1本足す | S |
| 4 | **兄妹の掛け合い**。同卓に姚玖がいる局の頭で一往復だけ（春嬋「兄さん、遅いっ」／姚玖「……先に行け」）。CPU同士でも出す | 固有性 | `pairWith` と同じ発想の cond を局頭セリフに追加（`voiceLines.js` の `condMatches` に1行） | S |

> **なぜ効くか**：「速い」は絵で描くと嘘になる（実際の待ち時間は変わらない）ので、**実際に速くする**。プレイヤーの手番リズムが変わるのが最大の演出。

> **実装メモ**：テンポ短縮は `cpuDelayNow()`（既定650ms → 発動中380ms → 疾走バッジで260ms）。ゲームロジックには一切触れず、CPU の見せ待ちだけを詰める。足跡は絵文字ではなく翠のシェブロン描画（絵文字は色がテーマから浮くため）。

---

### 2-3. ルクス・ゼロ「ゼロ・リサーチ」— 計器で、山を照らす  ✅実装済み

無機質・語彙は「捕捉／確保／誤差／再計算」。現状の候補バーは**素のボタン列＝人格ゼロ**。ここは既存UIの置き換えだけで最も費用対効果が高い。

| # | 演出 | 原理 | 実装アンカー | コスト |
|---|---|---|---|---|
| 1 | **候補選択を計器モーダルに**。黒地＋等幅＋シアン。上に `LUX SCAN — 残牌 47 / 走査完了`、候補ボタンに待ち枚数（`breadth`）と残枚数を数値表示 | 固有性 | `showLuxCandidates`（src/main.js:4872）を置換。データは `game.abilityStatus` の `candidates` に既にある | S |
| 2 | **予約 → 回収**。選択した瞬間、山（壁牌）の一点に光点が刺さる。次のツモでその光点が手牌へ飛び込む＝**能力が効いた瞬間が初めて見える** | 固有性 | `playLuckyTsumoFx` と同型の2段FX（予約点＝卓上固定、回収＝手牌へ移動） | M |
| 3 | **NOT FOUND の格上げ**。有効牌が山に無いとき、現状は12pxの淡い注記。→ 計器に `該当なし — 場に出切っている` と大きく出し、**河の該当牌が一瞬光る**（＝証明を見せる） | 反転 | 既存の `luxGrayHint`（src/main.js:4860）を昇格＋河ハイライト（`setHighlights` に1キー追加） | M |
| 4 | **超越帯（Lv9+「誤差の一打」）**。計器にノイズが走り `ERROR: 該当なし` → 一拍おいて `——構わない` に書き換わる | 反転 | `fallbackDraw` は実装済み（`_fallbackMode` フラグあり）。演出だけで成立 | S |

> **なぜ効くか**：#3 が彼のキャラが最も立つ瞬間。「能力が使えない」を**情報の贈り物**に反転させる＝無機質な男の示す優しさ。失敗が体験として面白くなる稀なケース。

> **実装メモ**：計器の数値（待ち幅・山の残り枚数・出切っている種類）は能力側の `scanReport()` が返す。`zeroSearchEffectiveKinds` は重いので `_scan()` で1回だけ走らせて `liveCandidates` と共用する（毎 render に二度引かない）。

---

### 2-4. 篠宮 栞「模範解答」— 卓を、教室にする  ✅実装済み

敬語・「わたし」／「あなた」・教師の語彙。素性＝**教える側に居続け、正解のない一打の前に立ったことがない**負い目。
能力が「プレイヤーを賢くする道具」なので、放っておくと**キャラがいなくても成立してしまう**。"誰が見ているか" を足すのが処方箋。

| # | 演出 | 原理 | 実装アンカー | コスト |
|---|---|---|---|---|
| 1 | **①②③を"栞"の意匠に**。丸数字 → 牌の上端に挟まる紙のしおり＋垂れるリボン。名前とメカが一致する | 固有性 | `canvasRenderer._bestMark`（src/ui/canvasRenderer.js:466 付近）の描画差し替えのみ | S |
| 2 | **板書HUDを黒板に**。緑黒板＋チョーク書体、隅にチョークを持つ彼女のミニ立ち絵 | 固有性 | `updateModelAnswerHud` の `ma-block` に装飾（CSS中心） | S |
| 3 | **推奨外を切った瞬間は無反応**（黙って見守る）。触れるのは局が終わってから | ― | 実装不要（我慢する設計判断） | ― |
| 4 | **「答え合わせ」＝彼女だけの対局後モーダル**。一致率の成績表ではなく、**外した一打を1つだけ**取り上げて彼女が語る：<br>「あなたはここで、わたしが選ばなかった牌を切りました。……結果は、あなたの勝ちでした」 | 蓄積＋反転 | 対局中に「推奨外の一打」を1件だけ記録 → 対局終了モーダルに差す | M |

> **なぜ効くか**：#4 が本丸。**正解のない一打の前で立ち止まる彼女**というアークを、対局のたび1行ずつ滲ませられる。
> 助言キャラが助言を裏切られる場面こそ、彼女の物語が動く場所になる。

> **実装メモ**：外した一打は打牌が**確定する直前**（`applyTurnDecision`）に模範解答と突き合わせて記録する（打牌後だと13枚になり模範解答が消えるため）。オート代行中の打牌は "あなたの選択" ではないので数えない。局の結末（和了／放銃／流局）を紐づけて文面を出し分け、外した一打が無い対局にも専用の一言を返す。

---

### 2-5. 詩玥（既存）— 足すなら1点だけ

- リングの色をラス目で鈍色に落とす（金→灰）。口癖「ツモれば勝ち」の**意味の反転**（軽口→呪い）に接続できる。
- **今回は優先度低**。他4人の空白を埋めるのが先。

---

## 3. 先に作ると全キャラに効く「器」

### 3-1. 能力の持続レイヤー（横断）  ✅実装済み

発動中の状態を卓の一角に常時表示する共通枠。中身だけキャラ別に差す。

- 詩玥＝金の輪／姚玖＝庭と灯籠／春嬋＝足跡と風／ルクス＝走査線
- **これが無いと (A) の不足は毎キャラ個別実装になる**。姚玖・春嬋の実装時にこの器として切り出すのが最も安い。
- 置き場の候補：`self-abilities` バッジ（src/main.js:7238 `renderPassiveBadges`）の拡張、または卓の下辺に新レイヤー。

### 3-2. 「やられた側」の可視化（横断）  ✅実装済み

現状、敵CPUの能力は**カットインで名前が出るだけ**で、何をされたか分からない。
→ 効果が自分に及んだ瞬間に一行テロップ（例：「あなたの手が縛られた — 沈黙の処方箋」）。

- アタッカー陣に限らないが、**敵として遭遇するキャラの印象**を作る最短路。
- 現状これが無いため、CPUのキャラは「立ち絵と名前」以上の記憶が残らない。

---

## 4. 実装順（すべて完了）

1. ✅ **姚玖の庭 ＋ 春嬋の足跡**（§3-1 の持続レイヤーを器として切り出しながら実装）
2. ✅ **ルクスの計器モーダル ＋ 光点の回収 ＋ NOT FOUND**
3. ✅ **栞のしおりマーク・黒板 ＋ 答え合わせモーダル**
4. ✅ **§3-2 やられた側の一行**（能力カットインの副題として実装）

**次**：第2弾＝ブロッカー（凌雲・真守・ビビ・エージェントRE）／ギャンブラー（ドラニエル・焔・ルイナ・沼田蓮）／アビス（ネビュラ・JaneDoe・カリュブディス）。
§3-1 の器があるので、持続型は `sync◯◯Aura()` を1本足して `updateAbilityAura()` に1行、で増やせる。

> テンポの上限：`docs/juice_backlog.md` の運用メモに同じ。連戦のテンポを殺さないこと。持続レイヤーは**発動局のみ**、単発FXは 0.6s 以内に収める。


---

## 5. 実装メモ（どこに何があるか）

### 共通の器
| もの | 場所 |
|---|---|
| 持続レイヤーのホスト | `index.html` の `#ability-aura`（卓の左下） |
| 器の出し入れ | `src/main.js` `ensureAuraPanel` / `clearAbilityAura` / `updateAbilityAura`（`render()` から毎回同期） |
| 1局ぶんの状態 | `src/main.js` `auraFx`（`kind` / `seen` / `lanterns` / `moon` / `steps` / `rush`）。局頭 `HAND_STARTED` で `resetAbilityAuraState()` |
| 牌に重ねる小FX | `src/main.js` `playTileFx(tileId, variant)`。`""`＝金(詩玥) / `fx-lantern`＝橙(姚玖) / `fx-wind`＝翠(春嬋) / `fx-scan`＝シアン(ルクス) |

**新しいキャラの持続演出を足す手順**：`sync◯◯Aura()` を1本書く → `updateAbilityAura()` に1行足す → CSS に `.aura-◯◯` を書く。以上。

### キャラ別
| キャラ | 主な関数 / 場所 |
|---|---|
| 姚玖 | `syncGardenAura` / `collectYaochuKinds`（`src/main.js`）、CSS `.aura-garden` `.lantern` `.garden-moon` `.table-wrap.aura-night` |
| 春嬋 | `syncSprintAura` / `bumpSprintStep` / `playSprintFlash` / `cpuDelayNow`（`src/main.js`）、CSS `.aura-sprint` `.step-mark` `.step-call` `.sprint-flash` |
| 卓上の掛け合い | 文言＝`characterVoiceMaster` の `tableBanter` / `tableBanterReply`（`cond.pairWith`）。出す判断だけ `maybeTableBanter` / `showSpeakerText`（`src/main.js`）。局頭から1対局1回。**姚玖×春嬋に限らず、マスタに両者ぶん書けばどのペアでも成立する** |
| ルクス | `showLuxCandidates`（計器） / `playLuxReserveFx` / `maybePlayLuxCaptureFx` / `updateLuxWatch`（`src/main.js`）、走査データは `ZeroSearchAbility.scanReport`（`src/abilities/builtins/drawAbilities.js`）→ `abilityStatus().scan` 経由。CSS `.lux-scan` `.lux-alert` `.lux-point` |
| 栞 | しおり＝`CanvasRenderer._bestMark`、黒板＝CSS `.model-advice .ma-block`、答え合わせ＝`noteShioriChoice` / `noteShioriResult` / `showShioriReview`（`src/main.js`）＋ CSS `.shiori-review`。**文言は `characterVoiceMaster` の `review`**（`cond.reviewMiss` × `cond.lastHandResult` で分岐・`{tile}` を表示側で差し替え） |
| やられた側の一行 | `abilityMaster.enemyNote`（`{name}` / `{target}` のテンプレ）→ `enemyNoteFor` → `showAbilityCutIn` の `subLabel`。`ABILITY_USED` に `abilityId` / `params` を載せて実現（`src/core/game.js`） |

### セリフはマスタ駆動（CLAUDE.md の規定どおり）
掛け合いも答え合わせも**文言は `src/data/characterVoiceMaster.js`** にあり、`src/main.js` は「誰が・いつ喋るか」だけを持つ。
条件キーは `voiceLines.js` の `condMatches` に1行足す方式（今回 `cond.reviewMiss` を追加）。
`cond.pairWith` は楼光のボス口上（`rlBossIntroPair` / `rlBossIntroPairReply`）と同じ仕組みの再利用。

### 制約
- いずれも**自分視点のオフライン演出**（`online` 中は出さない）。オンラインはレプリカで能力を回さないため。
- 単発FXは 0.6s 以内、持続表示は発動局のみ＝連戦テンポを殺さない（`docs/juice_backlog.md` の運用メモと同じ方針）。
- オンラインの `applyEvent` は `abilityId` を配信していないので、敵能力の副題はオフラインのみ出る（将来やるならイベント側の拡張が要る）。

---

## 6. QA で直した点（2026-08-23）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🔴 | 「答え合わせ」モーダルが対局開始でリセットされず、`setTimeout` のハンドルも持っていない。大会の節送りや対戦ホーム復帰（リロードを挟まない連戦）で、前の対局の答え合わせが次の対局に割り込む | `resetShioriReview()` を追加し、局頭（`HAND_STARTED`）と対局開始（`setupMatchTalk`）で畳む＋予約タイマーを解除 |
| 🔴 | 能力カットイン副題の `max-width: 70vw` が固定1280×720ステージ規約に違反（`vw` はステージの `scale` と無関係） | `calc(var(--stage-w) * 0.55)` に変更 |
| 🔴 | 掛け合い・答え合わせのセリフが `main.js` 直書きで、マスタ駆動の規定に反していた | `characterVoiceMaster` へ移設（`tableBanter` / `tableBanterReply` / `review`）。`condMatches` に `reviewMiss` を1行追加 |
| 🟡 | `updateLuxWatch()` が毎 render で `abilityStatus()`（34×34のシャンテン評価）を叩く | 「自分が打牌を選ぶ手番」のときだけ評価するようガード |
| 🟡 | JaneDoe の強制ツモ切り中の打牌が「あなたが選んだ一打」として答え合わせに乗る | `forcedTsumogiri > 0` の間は記録しない |
| 🟡 | `#ability-aura`（卓左下）が他モードのUIと衝突しないか未確認 | ペア戦で実測し、重なる可視要素なしを確認 |
