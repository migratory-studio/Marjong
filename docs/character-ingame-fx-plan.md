# キャラ別インゲーム演出

> | 弾 | 対象 | 状況 |
> |---|---|---|
> | 第1弾 | アタッカー（詩玥・ルクス・姚玖・春嬋・栞） | **実装完了**（2026-08-23）。§5 に実装メモ、§7 に対人戦の扱い |
> | 第2弾 | ブロッカー（凌雲・真守・ビビ・エージェントRE） | **実装完了**（2026-08-24）。設計＝§8、実装メモ＝§9 |
> | 第3弾 | ギャンブラー／アビス | 未着手 |

> 判断軸は CLAUDE.md（愛着4原理＝**蓄積 × 固有性 × 双方向 × 反転**／点棒=HPの体感）。
> オートバトル側のジュース候補は `docs/juice_backlog.md`。本書は**対局中（本気対局）でキャラが立つか**だけを扱う。
> 対象＝アタッカー5名（詩玥・ルクス・ゼロ・姚玖・春嬋・篠宮 栞）。ブロッカー／ギャンブラー／アビスは第2弾以降。

---

## 0. 現状の棚卸し（実装の事実）

| キャラ | 能力 | いま卓上で起きていること |
|---|---|---|
| 詩玥 | 幸運のツモ（発動局は毎ツモ有利牌へ偏る） | 発動カットイン → **毎ツモ、ツモ牌に金のリングFX**（`playLuckyTsumoFx` src/main.js:6383）→ 局中セリフ |
| ルクス・ゼロ | ゼロ・リサーチ（1シャンテン→確定聴牌） | カットイン → action-bar に候補ボタン2つ（`showLuxCandidates` src/main.js:4872）→ 次ツモで牌が来る（**来た瞬間は無演出**） |
| 姚玖 | 老頭の庭（么九に偏る） | カットイン **のみ**。以後、局が終わるまで何も起きない |
| 春嬋 | 韋駄天の中張（2〜8に偏る） | カットイン **のみ**。以後、局が終わるまで何も起きない |
| 篠宮 栞 | 模範解答（有効打トップ3を①②③） | 手牌に丸数字（`_bestMark`）＋Lv7以上で左上に板書HUD（`updateModelAnswerHud` src/main.js:5017） |

### 不足の正体は3種類ある（処方箋が違う）

- **(A) 持続が見えない** … 姚玖・春嬋。発動したのに世界が変わらない。「使ったのに何も起きなかった」体験。
- **(B) 因果が見えない** … ルクス。選択はできるが、**選んだ牌が来た瞬間**＝回収のカタルシスが無い。
- **(C) 機能はあるがキャラがいない** … 栞。誰が作ってもいい汎用UI。能力が便利なほどキャラが消える。

詩玥だけが (A) を解けている＝金リング。**「詩玥だけ実装されている」という体感は正しい**。

---

## 1. 今回の物差し

> **能力＝そのキャラが見ている世界を、一局だけプレイヤーに貸す。**

演出を「効果の説明」ではなく「**視界の貸し出し**」として作る。そうすると愛着4原理の *固有性* が自動的に立つ。

- 詩玥 ＝「ツモは、光る」
- 姚玖 ＝「端の牌は、灯り」
- 春嬋 ＝「卓は、駆ける道」
- ルクス ＝「山は、走査対象」
- 栞 ＝「卓は、教室」

**禁じ手**：効果の数値をそのまま出すこと（「么九率+40%」等）。世界の貸し出しにならず、キャラが消える。

---

## 2. キャラ別プラン

### 2-1. 姚玖「老頭の庭」— 夜の庭に、灯がともる  ✅実装済み

一人称「俺」・朴訥クール・口癖「揃うと、いいな」。**庭番（先代の養子）＝守る側**。能力は短期戦で当たりにくい仕様（だから初期16,000）。
つまり**当たらなくても進捗が見える装置**が要る。灯が増えること自体を報酬にする。

| # | 演出 | 原理 | 実装アンカー | コスト |
|---|---|---|---|---|
| 1 | **発動＝卓が夜になる**。四隅がすっと暗く落ち、自分の手牌の下辺に石畳＋灯籠の細い帯（庭）が出る。局が終わるまで残る＝持続の器 | 固有性 | DOM オーバーレイ（`tsumo-fx` と同じ position:fixed 方式）＋ CSS | M |
| 2 | **么九を引くたび灯が1つともる**。手牌の么九「種類数」＝灯籠の数。点灯の瞬間、その牌に橙のリング（詩玥の金と対の色） | 蓄積 | `maybePlayLuckyTsumoFx` と同型の hook（ツモ後に種類数を数えて差分点灯） | S |
| 3 | **夢の射程が見える**。么九が9種を超えると庭全体が明るくなる。国士テンパイで**満月**が昇り、「……揃うと、いいな」のテロップ | 反転 | `showSeatCall`（src/main.js:5493）流用＋庭レイヤーの状態切替 | S |
| 4 | **局後1行**：「今夜の灯り ◯／13」。派手にせず、静かに1行だけ | 蓄積 | 和了/流局の結果表示に1行差す | S |

> **なぜ効くか**：姚玖の能力は「当たらないこと」が仕様に織り込まれている。従来は当たらない＝無。灯なら**引けなかった局も庭が暗いまま**という語りになる。夢追いの体感がメカと一致する。

---

### 2-2. 春嬋「韋駄天の中張」— 走った距離が、見える  ✅実装済み

一人称「うち」・早口きびきび・口癖「間に合わせる」。姚玖を「兄さん」と呼ぶ。
中張が来ること自体は**当たり前すぎて気づけない**。だから「速度」という別チャネルに翻訳する。**時間で体感させる**のは他キャラに無い軸。

| # | 演出 | 原理 | 実装アンカー | コスト |
|---|---|---|---|---|
| 1 | **ツモが速くなる**。発動中は自分のツモ牌のスライドインを短縮＋牌の後ろに細い風ライン。ツモSEも半音上げ | 固有性 | 描画側のツモ演出タイミング＋`audio.playSe` の差し替え | M |
| 2 | **足跡カウンタ**。シャンテンが進むたび席の横に「一歩」「二歩」…と足跡テロップ。3歩連続で「韋駄天」バッジ点灯、以後さらにテンポが詰まる | 蓄積 | `spawnCall`（src/main.js:5482）＋ `humanShanten()`（src/main.js:8180）の差分監視 | S |
| 3 | **聴牌の瞬間**：「——間に合った」＋風が抜ける（画面端を横切る一閃）。口癖の回収 | 反転 | 既存 `tenpai` イベント（`setupMatchTalk`）にFXを1本足す | S |
| 4 | **兄妹の掛け合い**。同卓に姚玖がいる局の頭で一往復だけ（春嬋「兄さん、遅いっ」／姚玖「……先に行け」）。CPU同士でも出す | 固有性 | `pairWith` と同じ発想の cond を局頭セリフに追加（`voiceLines.js` の `condMatches` に1行） | S |

> **なぜ効くか**：「速い」は絵で描くと嘘になる（実際の待ち時間は変わらない）ので、**実際に速くする**。プレイヤーの手番リズムが変わるのが最大の演出。

> **実装メモ**：テンポ短縮は `cpuDelayNow()`（既定650ms → 発動中380ms → 疾走バッジで260ms）。ゲームロジックには一切触れず、CPU の見せ待ちだけを詰める。足跡は絵文字ではなく翠のシェブロン描画（絵文字は色がテーマから浮くため）。

---

### 2-3. ルクス・ゼロ「ゼロ・リサーチ」— 計器で、山を照らす  ✅実装済み

無機質・語彙は「捕捉／確保／誤差／再計算」。現状の候補バーは**素のボタン列＝人格ゼロ**。ここは既存UIの置き換えだけで最も費用対効果が高い。

| # | 演出 | 原理 | 実装アンカー | コスト |
|---|---|---|---|---|
| 1 | **候補選択を計器モーダルに**。黒地＋等幅＋シアン。上に `LUX SCAN — 残牌 47 / 走査完了`、候補ボタンに待ち枚数（`breadth`）と残枚数を数値表示 | 固有性 | `showLuxCandidates`（src/main.js:4872）を置換。データは `game.abilityStatus` の `candidates` に既にある | S |
| 2 | **予約 → 回収**。選択した瞬間、山（壁牌）の一点に光点が刺さる。次のツモでその光点が手牌へ飛び込む＝**能力が効いた瞬間が初めて見える** | 固有性 | `playLuckyTsumoFx` と同型の2段FX（予約点＝卓上固定、回収＝手牌へ移動） | M |
| 3 | **NOT FOUND の格上げ**。有効牌が山に無いとき、現状は12pxの淡い注記。→ 計器に `該当なし — 場に出切っている` と大きく出し、**河の該当牌が一瞬光る**（＝証明を見せる） | 反転 | 既存の `luxGrayHint`（src/main.js:4860）を昇格＋河ハイライト（`setHighlights` に1キー追加） | M |
| 4 | **超越帯（Lv9+「誤差の一打」）**。計器にノイズが走り `ERROR: 該当なし` → 一拍おいて `——構わない` に書き換わる | 反転 | `fallbackDraw` は実装済み（`_fallbackMode` フラグあり）。演出だけで成立 | S |

> **なぜ効くか**：#3 が彼のキャラが最も立つ瞬間。「能力が使えない」を**情報の贈り物**に反転させる＝無機質な男の示す優しさ。失敗が体験として面白くなる稀なケース。

> **実装メモ**：計器の数値（待ち幅・山の残り枚数・出切っている種類）は能力側の `scanReport()` が返す。`zeroSearchEffectiveKinds` は重いので `_scan()` で1回だけ走らせて `liveCandidates` と共用する（毎 render に二度引かない）。

---

### 2-4. 篠宮 栞「模範解答」— 卓を、教室にする  ✅実装済み

敬語・「わたし」／「あなた」・教師の語彙。素性＝**教える側に居続け、正解のない一打の前に立ったことがない**負い目。
能力が「プレイヤーを賢くする道具」なので、放っておくと**キャラがいなくても成立してしまう**。"誰が見ているか" を足すのが処方箋。

| # | 演出 | 原理 | 実装アンカー | コスト |
|---|---|---|---|---|
| 1 | **①②③を"栞"の意匠に**。丸数字 → 牌の上端に挟まる紙のしおり＋垂れるリボン。名前とメカが一致する | 固有性 | `canvasRenderer._bestMark`（src/ui/canvasRenderer.js:466 付近）の描画差し替えのみ | S |
| 2 | **板書HUDを黒板に**。緑黒板＋チョーク書体、隅にチョークを持つ彼女のミニ立ち絵 | 固有性 | `updateModelAnswerHud` の `ma-block` に装飾（CSS中心） | S |
| 3 | **推奨外を切った瞬間は無反応**（黙って見守る）。触れるのは局が終わってから | ― | 実装不要（我慢する設計判断） | ― |
| 4 | **「答え合わせ」＝彼女だけの対局後モーダル**。一致率の成績表ではなく、**外した一打を1つだけ**取り上げて彼女が語る：<br>「あなたはここで、わたしが選ばなかった牌を切りました。……結果は、あなたの勝ちでした」 | 蓄積＋反転 | 対局中に「推奨外の一打」を1件だけ記録 → 対局終了モーダルに差す | M |

> **なぜ効くか**：#4 が本丸。**正解のない一打の前で立ち止まる彼女**というアークを、対局のたび1行ずつ滲ませられる。
> 助言キャラが助言を裏切られる場面こそ、彼女の物語が動く場所になる。

> **実装メモ**：外した一打は打牌が**確定する直前**（`applyTurnDecision`）に模範解答と突き合わせて記録する（打牌後だと13枚になり模範解答が消えるため）。オート代行中の打牌は "あなたの選択" ではないので数えない。局の結末（和了／放銃／流局）を紐づけて文面を出し分け、外した一打が無い対局にも専用の一言を返す。

---

### 2-5. 詩玥（既存）— 足すなら1点だけ

- リングの色をラス目で鈍色に落とす（金→灰）。口癖「ツモれば勝ち」の**意味の反転**（軽口→呪い）に接続できる。
- **今回は優先度低**。他4人の空白を埋めるのが先。

---

## 3. 先に作ると全キャラに効く「器」

### 3-1. 能力の持続レイヤー（横断）  ✅実装済み

発動中の状態を卓の一角に常時表示する共通枠。中身だけキャラ別に差す。

- 詩玥＝金の輪／姚玖＝庭と灯籠／春嬋＝足跡と風／ルクス＝走査線
- **これが無いと (A) の不足は毎キャラ個別実装になる**。姚玖・春嬋の実装時にこの器として切り出すのが最も安い。
- 置き場の候補：`self-abilities` バッジ（src/main.js:7238 `renderPassiveBadges`）の拡張、または卓の下辺に新レイヤー。

### 3-2. 「やられた側」の可視化（横断）  ✅実装済み

現状、敵CPUの能力は**カットインで名前が出るだけ**で、何をされたか分からない。
→ 効果が自分に及んだ瞬間に一行テロップ（例：「あなたの手が縛られた — 沈黙の処方箋」）。

- アタッカー陣に限らないが、**敵として遭遇するキャラの印象**を作る最短路。
- 現状これが無いため、CPUのキャラは「立ち絵と名前」以上の記憶が残らない。

---

## 4. 実装順（すべて完了）

1. ✅ **姚玖の庭 ＋ 春嬋の足跡**（§3-1 の持続レイヤーを器として切り出しながら実装）
2. ✅ **ルクスの計器モーダル ＋ 光点の回収 ＋ NOT FOUND**
3. ✅ **栞のしおりマーク・黒板 ＋ 答え合わせモーダル**
4. ✅ **§3-2 やられた側の一行**（能力カットインの副題として実装）

**次**：第2弾＝ブロッカー（凌雲・真守・ビビ・エージェントRE）／ギャンブラー（ドラニエル・焔・ルイナ・沼田蓮）／アビス（ネビュラ・JaneDoe・カリュブディス）。
§3-1 の器があるので、持続型は `sync◯◯Aura()` を1本足して `updateAbilityAura()` に1行、で増やせる。

> テンポの上限：`docs/juice_backlog.md` の運用メモに同じ。連戦のテンポを殺さないこと。持続レイヤーは**発動局のみ**、単発FXは 0.6s 以内に収める。


---

## 5. 実装メモ（どこに何があるか）

### 共通の器
| もの | 場所 |
|---|---|
| 持続レイヤーのホスト | `index.html` の `#ability-aura`（卓の左下） |
| 器の出し入れ | `src/main.js` `ensureAuraPanel` / `clearAbilityAura` / `updateAbilityAura`（`render()` から毎回同期） |
| 1局ぶんの状態 | `src/main.js` `auraFx`（`kind` / `seen` / `lanterns` / `moon` / `steps` / `rush`）。局頭 `HAND_STARTED` で `resetAbilityAuraState()` |
| 牌に重ねる小FX | `src/main.js` `playTileFx(tileId, variant)`。`""`＝金(詩玥) / `fx-lantern`＝橙(姚玖) / `fx-wind`＝翠(春嬋) / `fx-scan`＝シアン(ルクス) |

**新しいキャラの持続演出を足す手順**：`sync◯◯Aura()` を1本書く → `updateAbilityAura()` に1行足す → CSS に `.aura-◯◯` を書く。以上。

### キャラ別
| キャラ | 主な関数 / 場所 |
|---|---|
| 姚玖 | `syncGardenAura` / `collectYaochuKinds`（`src/main.js`）、CSS `.aura-garden` `.lantern` `.garden-moon` `.table-wrap.aura-night` |
| 春嬋 | `syncSprintAura` / `bumpSprintStep` / `playSprintFlash` / `cpuDelayNow`（`src/main.js`）、CSS `.aura-sprint` `.step-mark` `.step-call` `.sprint-flash` |
| 卓上の掛け合い | 文言＝`characterVoiceMaster` の `tableBanter` / `tableBanterReply`（`cond.pairWith`）。出す判断だけ `maybeTableBanter` / `showSpeakerText`（`src/main.js`）。局頭から1対局1回。**姚玖×春嬋に限らず、マスタに両者ぶん書けばどのペアでも成立する** |
| ルクス | `showLuxCandidates`（計器） / `playLuxReserveFx` / `maybePlayLuxCaptureFx` / `updateLuxWatch`（`src/main.js`）、走査データは `ZeroSearchAbility.scanReport`（`src/abilities/builtins/drawAbilities.js`）→ `abilityStatus().scan` 経由。CSS `.lux-scan` `.lux-alert` `.lux-point` |
| 栞 | しおり＝`CanvasRenderer._bestMark`、黒板＝CSS `.model-advice .ma-block`、答え合わせ＝`noteShioriChoice` / `noteShioriResult` / `showShioriReview`（`src/main.js`）＋ CSS `.shiori-review`。**文言は `characterVoiceMaster` の `review`**（`cond.reviewMiss` × `cond.lastHandResult` で分岐・`{tile}` を表示側で差し替え） |
| やられた側の一行 | `abilityMaster.enemyNote`（`{name}` / `{target}` のテンプレ）→ `enemyNoteFor` → `showAbilityCutIn` の `subLabel`。`ABILITY_USED` に `abilityId` / `params` を載せて実現（`src/core/game.js`） |

### セリフはマスタ駆動（CLAUDE.md の規定どおり）
掛け合いも答え合わせも**文言は `src/data/characterVoiceMaster.js`** にあり、`src/main.js` は「誰が・いつ喋るか」だけを持つ。
条件キーは `voiceLines.js` の `condMatches` に1行足す方式（今回 `cond.reviewMiss` を追加）。
`cond.pairWith` は楼光のボス口上（`rlBossIntroPair` / `rlBossIntroPairReply`）と同じ仕組みの再利用。

### 制約
- 単発FXは 0.6s 以内、持続表示は発動局のみ＝連戦テンポを殺さない（`docs/juice_backlog.md` の運用メモと同じ方針）。
- 演出は**自席の描画だけ**を触る。盤面・進行・乱数には一切干渉しない。

---

## 7. 対人戦（オンライン）での扱い ✅実装済み

演出の実体が「進行速度」である春嬋だけが、対人戦で構造的な問題を持つ。オンラインの進行ペースは
権威（`AuthorityRoom._pace`）が握っていて、そこを能力で動かすと**ホストのクライアント都合で卓全員の
速度が変わる**（CPU代行席の速度も変わる）＝公平性と同期の問題になる。そこで次の3本をルールにした。

| # | ルール | 該当 |
|---|---|---|
| 1 | **自席の情報だけで描ける演出は、対人戦でも出す** | 姚玖の庭・春嬋の足跡/一閃・卓上の掛け合い・ルクスの捕捉→回収・NOT FOUND・栞の答え合わせ・詩玥の金リング |
| 2 | **進行速度に触る演出は、オフライン専用** | 春嬋のテンポ短縮（`cpuDelayNow` は `online` で即 `CPU_DELAY` を返す）。対人戦での"速さ"は視覚（足跡・風・オーラ）で語る |
| 3 | **他家が絡む演出は、権威が送っている範囲でだけ出す** | 敵能力カットインの副題。`evt.abilityUsed` に `abilityId` と `targetSeat` を追加 |

### 実装の要点
- **発動中の判定**：`humanAbilityActive()` はオンラインでは権威が `evt.awaitDiscard` で自席にだけ送る
  `abilityStatus`（`sendToSeat` 経路）の `active` を見る。自分の手番のたび更新され、局頭リセットと
  合わせて「発動した局のあいだ立っている」状態を再現する。
- **能力の所持判定**：レプリカは能力インスタンスを持たないので `character.abilities` で見る（`humanHasAbility`）。
- **ツモ演出の入口**：オフラインは `waitHumanTurn` / `autoTsumogiri` / `fireAbility`、オンラインは
  `evt.awaitDiscard` 受信時（`onlineClientMessage`）。
- **栞の答え合わせ**：オンラインは `applyTurnDecision` を通らないので `resolveHumanTurn` の online 分岐で
  突き合わせる。模範解答自体は `localModelAnswer`（クライアント計算）なので対人戦でも出る。
- **★秘匿**：`evt.abilityUsed` に `params` を丸ごと載せてはいけない。**ゼロ・リサーチの `targetKind`
  （＝次に引く牌）が他家に漏れる**。公開してよいのは `abilityId` と `targetSeat`（誰を対象にしたか）だけ。
  `test/eventlog.mjs` に、この2点（abilityId が届く／targetKind がどこにも載らない）の回帰テストがある。
- 栞の板書HUD（Lv7+）だけは対人戦で出ない：`comebackPlan` / `pushFoldAdvice` は権威が送っていないため
  （ルール3のとおり、送られていない情報は出さない）。

---

## 6. QA で直した点（2026-08-23）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🔴 | 「答え合わせ」モーダルが対局開始でリセットされず、`setTimeout` のハンドルも持っていない。大会の節送りや対戦ホーム復帰（リロードを挟まない連戦）で、前の対局の答え合わせが次の対局に割り込む | `resetShioriReview()` を追加し、局頭（`HAND_STARTED`）と対局開始（`setupMatchTalk`）で畳む＋予約タイマーを解除 |
| 🔴 | 能力カットイン副題の `max-width: 70vw` が固定1280×720ステージ規約に違反（`vw` はステージの `scale` と無関係） | `calc(var(--stage-w) * 0.55)` に変更 |
| 🔴 | 掛け合い・答え合わせのセリフが `main.js` 直書きで、マスタ駆動の規定に反していた | `characterVoiceMaster` へ移設（`tableBanter` / `tableBanterReply` / `review`）。`condMatches` に `reviewMiss` を1行追加 |
| 🟡 | `updateLuxWatch()` が毎 render で `abilityStatus()`（34×34のシャンテン評価）を叩く | 「自分が打牌を選ぶ手番」のときだけ評価するようガード |
| 🟡 | JaneDoe の強制ツモ切り中の打牌が「あなたが選んだ一打」として答え合わせに乗る | `forcedTsumogiri > 0` の間は記録しない |
| 🟡 | `#ability-aura`（卓左下）が他モードのUIと衝突しないか未確認 | ペア戦で実測し、重なる可視要素なしを確認 |

---

# 第2弾：ブロッカー陣（設計）

## 8-0. 現状の棚卸し（実装の事実）

| キャラ | 能力 | いま卓上で起きていること |
|---|---|---|
| 凌雲 | 琥珀の盾（常時・大物手だけ受け切る） | 常設バッジに **盾メーター ●○**（`uiState`）。ただし**受け止めた瞬間は `api.log()` の1行だけ** |
| 真守 由紀 | 放銃、いたしません（常時・危険牌を3段階警告） | 手牌に赤/橙/黄のオーバーレイ（`_dangerOverlay`）。**「避けた成果」は一切見えない** |
| ビビ | 身代わり人形（6打牌のあいだ無効化） | 発動カットインのみ。**残り打牌数（守りの窓）が完全に不可視**（`uiState` を持たない）。無効化の瞬間もログ1行 |
| エージェント・RE | リコール・ディール（河↔手牌の交換） | 河ハイライト＋クリック選択（UIはある）。**交換の瞬間の演出は無し** |

### 決定的な事実：守り切った席は、被ダメ演出から消える

`showDamageFx`（src/main.js）は登場者をこう決めている：

```js
const order = [r.winner];
game.players.forEach((p, i) => { if (i !== r.winner && deltas[i]) order.push(i); });
```

**`deltas[i]` が 0 の席は描かれない。** つまり凌雲が盾で受け切った局・ビビが無効化した局は、
**守り切った本人が、最大の見せ場で画面から消える**。起きているのはログの1行だけ。

これがブロッカーが地味に見える正体で、キャラの問題ではなく**演出基盤の構造**の問題。
既存の演出は「点棒が動く＝事件」を前提に組まれているので、**動かないことが成果**の
ブロッカーは設計上こぼれ落ちる。

---

## 8-1. 物差し（ブロッカー編）

第1弾の物差し（能力＝そのキャラの視界を一局だけ貸す）はそのまま維持する。そのうえで、
ブロッカーには課題の型が違うので、もう1本足す。

> **回避された未来を、一度だけ見せる。**
> 守りの演出は「本来どうなっていたか」を提示してから、それが消えるところまでを描いて初めて成立する。

- アタッカーの課題＝**効果が見えない**（何が起きたか分からない）
- ブロッカーの課題＝**成果が"起きなかったこと"**（何も起きないので見せる対象がない）

だから守りの演出は、**失われるはずだった点棒**を一瞬だけ実体化させる。
`-12000` が突き刺さり、琥珀に固まって砕け、実際の失点は 0——ここまでが1つの演出。
点棒＝HP という本作の核と、守備キャラの快感が初めて接続する。

**禁じ手**：「防御成功！」のような結果ラベルだけを出すこと。回避された未来が見えないと、
プレイヤーは何を免れたのか分からないまま終わる。

---

## 8-2. キャラ別プラン

### 8-2-1. 凌雲「琥珀の盾」— 受け切った額を、見せてから消す  ✅実装済み

寡黙・多くを語らず前に立つ。称号は天衣無縫。盾は**ゲームを通しての持続資源**（局をまたぐ）。

| # | 演出 | 原理 | 実装アンカー | コスト |
|---|---|---|---|---|
| 1 | **受け止めの瞬間**：被ダメ演出の頭で、本来の失点（例 `-12000`）が赤く突き刺さる → 琥珀色の膜が席を覆い、数字が琥珀に固まって砕ける → 実失点 0。**額を一度見せる**のが肝 | 反転 | §8-3-1 の「守り切った席」枠＋`showDamageFx` の行演出 | M |
| 2 | **盾が剥がれる瞬間**（閾値未満で被弾）：琥珀にヒビ、メーターが ●→○ に落ちる。既存ログ「……削られたか」と同期 | 蓄積 | `uiState.meter` は既にある。落ちるアニメを足すだけ | S |
| 3 | **編み直しの瞬間**（超越帯 Lv6+ の和了で盾+1）：琥珀が再生して満ちる。既存ログ「攻めることも、護りだ」と同期＝**攻めが護りに変わる**アークの可視化 | 反転 | `ON_WIN` の regen は実装済み。演出だけ | S |
| 4 | **相手側への一行**：琥珀で受けた分は勝者にも渡らない（`_settle` が差し引く）。ロンした側に「琥珀に阻まれた——点棒は動かない」を出す | 固有性 | §8-3-2 の「奪えなかった」枠 | S |

> **なぜ効くか**：盾は「1ゲームに1〜2回しか起きない代わりに、起きたら決定的」という設計。
> 回数が少ないぶん、その1回を事件にできる。#1 が無いと、最も効いた瞬間が最も静かなままになる。

### 8-2-2. ビビ「身代わり人形」— 人形が、代わりに砕ける  ✅実装済み

あどけない見た目／「誰にも奪わせない」意志。6打牌の窓のあいだ、**勝者すら点棒を得られない**。

| # | 演出 | 原理 | 実装アンカー | コスト |
|---|---|---|---|---|
| 1 | **窓の可視化**：発動中は残り打牌数を**人形の数**で表示（6体 → 打つたび1体減る）。持続レイヤーの器をそのまま使う | 蓄積 | `#ability-aura` ＋ `syncDollAura()` を1本。`BibiAbility` に `uiState`（残り窓）を追加 | S |
| 2 | **身代わりの瞬間**：人形が1体ヒビ割れて砕け、本来の失点が人形へ吸い込まれて消える。ビビは無傷のまま | 反転 | §8-3-1 の枠＋人形の破砕FX | M |
| 3 | **「奪わせない」の提示**：勝った側に「奪えなかった——点棒は誰のものにもならない」。能力の異様さはここでしか伝わらない | 固有性 | §8-3-2 の枠 | S |
| 4 | **窓が切れる瞬間**：最後の人形が静かに消える。ログ文言「守りが切れた」を彼女の言葉に差し替え | ― | `ON_DISCARD` の窓切れに1行 | S |

> **なぜ効くか**：いま**残り何打牌守られているのかが誰にも分からない**。押し引きの判断材料が
> 消えているので、機能としても損をしている。#1 は演出であると同時に UI の欠落を埋める。

### 8-2-3. 真守 由紀「放銃、いたしません」— 見えていたことを、後から示す  ✅実装済み

危険度は推定（`estimateDangerInfo` の3段階量子化）。警告は出るが、**当たっていたのか外れていたのか**が
プレイヤーに返らない。だから「この人の目は信用できる」が育たない。

| # | 演出 | 原理 | 実装アンカー | コスト |
|---|---|---|---|---|
| 1 | **「見えていた」の証明**：局が終わって手が開いたとき、真守が赤/橙で警告していた牌が**実際に当たり牌だった**なら、その牌に静かな印と一言。**外れた局は黙る** | 蓄積 | 和了/流局の結果で、警告履歴と実際の待ちを突き合わせる | M |
| 2 | **通す瞬間の緊張**：超危険（赤）の牌を切るときだけ一拍の間と心音。通ったら「……通りました」 | 双方向 | 打牌確定の直前（`applyTurnDecision` / `resolveHumanTurn`）にフック | S |
| 3 | **回避の集計**：流局時だけ「今局、危険牌を◯回回避」。毎局は出さない | 蓄積 | 局終了の結果表示に1行 | S |

> **なぜ効くか**：真守の能力は「情報を渡す」タイプで、渡した情報が正しかったかが返らないと
> ただの色になる。#1 は**キャラへの信頼**そのものを作る演出。口癖「放銃、いたしません」の
> 説得力もここで積み上がる。

### 8-2-4. エージェント・RE「リコール・ディール」— 回収の因果を見せる（locked）  ✅実装済み

寡黙な諜報型。ツモ牌を河へ流し、過去の捨て牌を手に戻す。宝珠ショップ解禁キャラ。

| # | 演出 | 原理 | 実装アンカー | コスト |
|---|---|---|---|---|
| 1 | **交換の瞬間**：選んだ河の牌が手牌へ飛び、ツモ牌が河へ滑り込む（2牌がすれ違う） | 固有性 | ルクスの光点（`tablePointAt` / `playLuxReserveFx`）と同型の器を流用 | M |
| 2 | **回収した牌に印**：手に戻った牌＝一度手放した取引材料。局のあいだ小さな諜報マーク | 蓄積 | `canvasRenderer` の手牌マーク（`_bestMark` と同じ層） | S |
| 3 | ~~`ronImmune` の可視化~~ **→ 不要（既に実装済みだった）**。`_ronImmuneMark` が河の該当牌に青い膜とバッジを描いている。設計時の見落としで、追加実装はしていない | ― | `canvasRenderer._ronImmuneMark` | ― |

---

## 8-3. 横断の器（ブロッカー編で作るもの）

### 8-3-1. 「守り切った席」を被ダメ演出に出す枠  ✅実装済み（本丸）

`showDamageFx` の登場者選定を拡張し、**delta が 0 でも「守った席」として描く**。
HPバーが減りかけて止まる／本来の失点が浮かんで消える、を共通の見せ方にする。

- 凌雲・ビビだけでなく、将来の防御系すべてに効く（第3弾のアビス勢にも）
- 「点棒が動く＝事件」しか描けない今の基盤の穴を塞ぐ
- **これを作らないと、ブロッカーの演出は毎回キャラ個別実装になる**

### 8-3-2. 「奪えなかった」側への一行  ✅実装済み

琥珀の盾・身代わり人形は**勝者の取り分も減らす**（`_settle` が軽減ぶんを差し引く）。
いまは説明が無いので、勝った側からは「満貫をロンしたのに点が入らない」＝バグに見える。
第1弾の `enemyNote`（能力カットインの副題）と同じ仕組みで、和了画面に一行を添える。

### 8-3-3. 守りの持続レイヤー（第1弾の器を流用）  ✅実装済み

ビビの人形6体は `sync◯◯Aura()` を1本足すだけ。器は既にある。

---

## 8-4. 実装順（すべて完了）

1. ✅ **§8-3-1 の「守り切った席」枠 ＋ 凌雲の受け止め演出**
2. ✅ **ビビの窓の可視化 ＋ 身代わりの瞬間**
3. ✅ **真守の「見えていた」証明**
4. ✅ **エージェントRE の交換演出**

**次**：第3弾＝ギャンブラー（ドラニエル・焔・ルイナ・沼田蓮）／アビス（ネビュラ・JaneDoe・カリュブディス）。
§8-3-1 の枠があるので、失点を軽減する系（ネビュラの逆＝倍化も含む）はそのまま乗る。

対人戦の扱いは §7 の3ルールをそのまま適用する。特に **§8-3-2 の「奪えなかった」一行は
他家に関わる表示**なので、ルール3（権威が送っている範囲でだけ出す）に従って、
和了結果に含まれる公開情報だけで組む。


---

## 9. 実装メモ（ブロッカー編：どこに何があるか）

| もの | 場所 |
|---|---|
| **守りの記録**（本来の失点） | `Game._settle` が `this._lastGuards = [{ seat, raw, adjusted, blocked }]` を作り、`lastResult.guards` に載せる（`src/core/game.js`）。wire event は `lastResult` を丸ごと運ぶので**オンラインへも自動で届く**（点棒の動きは公開情報） |
| 守り切った席の演出 | `showDamageFx`（`src/main.js`）。`order` に guard 席を足し、`is-guard` 行で「本来の失点 → 守りの膜 → 実際の増減」を順に見せる。CSS `.dmg-row.is-guard` / `.dmg-guard-veil` / `.dmg-pop.guard-raw` |
| 守りの名前と「奪えなかった」一行 | `abilityMaster` の `guardLabel` / `guardNote`（マスタ駆動）。CSS `.dmg-guard-tag` / `.dmg-blocked-note` |
| 凌雲の盾メーター増減 | `renderPassiveBadges` が `meterPrev` で前回値を覚え、変化時だけ `meter-break` / `meter-mend` を付ける。CSS `@keyframes shieldBreak` / `shieldMend` |
| ビビの守りの窓 | `BibiAbility.uiState()` が `meter{on,max}` を返す（`src/abilities/builtins/bibiAbility.js`）→ `syncDollAura()` が人形の数で描く。CSS `.aura-doll` / `.doll` |
| 真守の「見えていました」 | 警告履歴は `currentDanger()` が `mamoriWarned`（Map<kind,level>）へ蓄積 → `mamoriInsight(r)` が当たり牌と突き合わせ、`showDamageFx` に1行。文言は `characterVoiceMaster` の `insight`（`cond.dealtIn` で分岐・`{tile}` を差し替え） |
| エージェントRE の交換 | `playRecallSwapFx(riverTileId, drawnTileId)`（`src/main.js`）が2つの光をすれ違わせる。回収した牌は `recalledTileIds` → `CanvasRenderer._recallMark`。CSS `.recall-dot` |

### 設計との差分
- §8-2-4 #3（`ronImmune` の可視化）は**すでに実装済み**だった（`_ronImmuneMark`）。設計時の見落としで、追加実装はしていない。
- 真守の「回避の集計」（§8-2-3 #3・流局時に「危険牌を◯回回避」）は**見送り**。`mamoriInsight` の1行と役割が重なり、出しすぎになるため。必要になったら `mamoriWarned` から数えられる。

### 回帰テスト
- `test/ambershield.mjs` に「守り演出の材料」を追加（guards に載る／raw を保持／adjusted=0／blocked が出る／勝者の取り分も同額減る）。

---

## 10. QA で直した点（第2弾／2026-08-24）

| 重大度 | 指摘 | 対応 |
|---|---|---|
| 🔴 | 守り切った席で `return` し、HPバー更新・数字tween・**トビ（撃沈）演出**を丸ごと飛ばしていた。凌雲の超越帯（`stripMitigation>0`＝Lv7+）は「盾が剥がれつつ半額だけ受ける」＝軽減はされたが**実損が残る**ので、点棒が減っているのに表示が凍り、トビても撃沈演出が出ない | `return` を廃止し、守り演出を重ねたうえで通常処理（バー・数字・トビ）を必ず通す。守り行では `shake` と通常の数字ポップだけを抑制。`test/ambershield.mjs` に半額軽減ケースの回帰を追加 |
| 🟡 | 「奪えなかった」注記が `guards[0]` の1件だけを見ており、凌雲とビビが同時に守った局（ツモは支払者が複数）で内訳と食い違う | 守った席ごとに1行ずつ出す |
| 🟡 | 団体戦・ペア戦・楼光の館は別の被ダメ演出（`showTeamBattleDamageFx` / `showPairBattleDamageFx`）を使うため、「満貫をロンしたのに点が入らない」の無説明状態が残っていた。3人とも該当モードでプレイアブル | `guardBlockedNotesHtml(r)` を共通ヘルパーに切り出し、3つの被ダメ演出すべてに配線 |
| 🟡 | 真守の「見えていました」が**ツモ和了でも発火**（セリフは切る/避けるが前提）。また `estimateDangerInfo` は34種すべてを評価するため、**手牌に一度も来ていない牌**の警告にも反応していた | ロン和了限定にし、警告履歴は「実際に手牌にあった牌」だけを積むよう変更（＝手牌に色が付いた牌＝本人が判断した牌に限定） |
| 🟡 | 流局・流し満貫の `lastResult` に `guards` が無く、将来 draw 系に反応する守りが増えたとき黙って抜ける作り | `_exhaustiveDraw` / `_applyNagashi` にも `guards` を追加 |
| 🟡 | 守りの見た目が `id === "bibi"` のキャラ決め打ちで、第3弾で守り系が増えると破綻する | `abilityMaster.guardStyle`（"amber" / "doll"）へマスタ化 |
